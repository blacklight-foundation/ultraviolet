//! Reference specimens for Parallelism / PanicHandling.

/// Exercises the Parallelism PanicHandling reference surface.
public procedure runParallelismPanicHandlingReference() -> bool {
    let reference: ParallelReference = parallelReference(2usize, true, true)
    return parallelReferenceIsCovered(reference) && reference.propagates_panic
}
