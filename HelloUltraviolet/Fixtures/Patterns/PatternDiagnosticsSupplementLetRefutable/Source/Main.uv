//! Rejected pattern specimen for the refutable-let entry in the pattern diagnostic supplement.

public procedure patternDiagnosticsSupplementLetRefutableReference(value: i32) -> i32 {
    let 1 = value
    return value
}
