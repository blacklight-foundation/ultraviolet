//! Module documentation consumed by the lexer output fixture.

/// Line documentation consumed by the lexer output fixture.
public procedure lexerTokenizerOutputProbe() -> i32 {
    /* Ordinary block comment excluded from the token stream. */
    // Ordinary line comment excluded from the token stream.
    return 7
}
