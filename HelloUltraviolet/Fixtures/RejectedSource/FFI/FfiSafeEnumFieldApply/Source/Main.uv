//! Rejected FFI specimen for generic enum applications with substituted non-FfiSafe payloads.

#layout(C)
public enum GenericFfiEnum<TValue <: FfiSafe>
{
    Value(TValue)
}

extern "C" {
    public procedure importedEnum(value: GenericFfiEnum<bool>) -> i32
}
