//! Rejected direct record literal construction for a runtime-backed special modal state.

public procedure specialModalRecordLiteralRejected() -> File@Read {
    return File@Read { handle: 0usize }
}
