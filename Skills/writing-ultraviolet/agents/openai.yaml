interface:
  display_name: "Write Ultraviolet"
  short_description: "Author, review, and repair Ultraviolet code with primary pattern cards and bundled spec search"
  default_prompt: "Use $writing-ultraviolet to classify requested behavior into primary cards, fill the UV syntax holes, and verify against the bundled specification."
policy:
  allow_implicit_invocation: true
