//! Minimal Ultraviolet executable entry point.

public procedure main(context: Context) -> i32 {
    let output: Outcome<(), IoError> = context.fs~>write_stdout("hello ultraviolet\n")
    return if output is {
        @Value {
            0
        }
        @Error {
            1
        }
    }
}
